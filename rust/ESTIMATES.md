# Калибровка оценок

Оценки объёма работы здесь даются в **календарном времени** — от «делай» до пуша, включая
сборки, тесты и проверку в браузере, — и с указанием, кто делал: «(≈ 30 мин календарного
времени с Opus 5.5)». Эта таблица — чтобы оценки было с чем сверять: после каждой оценённой
заранее работы — строка.

Модель известна из окружения сессии; уровень усилий (effort) модель сама не видит — пишется
со слов пользователя, если он его назвал.

| дата | работа | оценка | начало → пуш | вышло | кто |
|---|---|---|---|---|---|
| 2026-09-25 | «Уточнить курс» в окне валют (план — бывший раздел BACKLOG «Курсы валют», путь 3): `rates.rs`, окно, id = ISO, справка, журнал, тесты, проверка в headless Chrome | «примерно день» | 22:27 → 22:49 CEST | ≈ 22 мин | Opus 5.5, effort high (со слов пользователя) |

**Вывод пока (одна точка):** оценка «день» писалась в человеко-днях и для этой работы
завышена раз в двадцать. Для работ, где план уже готов и решения приняты, оценивать в
десятках минут; хвосты — сборка wasm (~2 мин), пуск сервера со сборкой (~5 мин).
