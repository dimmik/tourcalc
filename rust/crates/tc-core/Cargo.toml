[package]
name = "tc-core"
version = "0.1.0"
edition = "2021"

# Whatever lands here lands in the browser, so the dependency list stays this short.
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
